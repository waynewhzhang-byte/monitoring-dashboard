# ==================== 数据库配置 ====================
# PostgreSQL 连接字符串
DATABASE_URL="postgresql://postgres:password@localhost:5432/monitoring-dashboard?schema=public"

# ==================== Redis 配置 ====================
# Redis 连接 URL（可选，用于缓存和实时通信）
REDIS_URL="redis://localhost:6379"

# ==================== OpManager API 配置 ====================
# OpManager 服务器地址
OPMANAGER_BASE_URL="https://opmanager.example.com"
# OpManager API Key（从 OpManager 管理界面获取）
OPMANAGER_API_KEY="your-api-key-here"
# API 请求超时（毫秒）
OPMANAGER_TIMEOUT="30000"
# 设备名后缀（getInterfaces 等 API 的 name 参数）
#   true  = 企业版：会额外尝试 name + ".10000000001"
#   false 或不设置 = 非企业版：不追加后缀（默认）
# OPMANAGER_USE_DEVICE_NAME_SUFFIX="true"
# getInterfaces 调用间隔（毫秒），避免 OpManager URL_ROLLING_THROTTLES_LIMIT_EXCEEDED。默认 30000（30 秒）
# OPMANAGER_GETINTERFACES_DELAY_MS="30000"

# ==================== 应用配置 ====================
# 运行环境: development | production | test
NODE_ENV="development"
# 服务端口
PORT="3000"
# 主机名
HOSTNAME="localhost"

# ==================== 数据采集配置 ====================
# 设备采集实时性能间隔（秒），默认 300（5 分钟），可后续调整
COLLECT_METRICS_INTERVAL="300"
# 接口采集间隔（秒），默认 300（5 分钟），可后续调整
COLLECT_INTERFACE_INTERVAL="300"
# 告警同步间隔（秒）
COLLECT_ALARMS_INTERVAL="30"
# 设备列表同步间隔（秒）
SYNC_DEVICES_INTERVAL="600"

# ==================== Mock Server 配置（开发环境） ====================
# 是否使用 Mock 数据（true/false）
USE_MOCK_DATA="false"
# Mock 数据更新间隔（分钟）
MOCK_UPDATE_INTERVAL_MINUTES="10"

# ==================== 日志配置 ====================
# 日志级别: error | warn | info | debug
LOG_LEVEL="info"

# ==================== 安全配置 ====================
# API Key（用于 API 认证，生产环境必须设置）
API_KEY=""
# JWT Secret（如果使用 JWT 认证）
JWT_SECRET=""

# ==================== 数据保留策略 ====================
# 历史数据保留天数
DATA_RETENTION_DAYS="30"

# ==================== WebSocket 配置 ====================
# WebSocket 服务器 URL（前端使用）
NEXT_PUBLIC_WS_URL="http://localhost:3000"

# ==================== 监控配置（可选） ====================
# Sentry DSN（错误追踪）
# SENTRY_DSN=""
# Sentry 环境
# SENTRY_ENVIRONMENT="production"
